bs4
selenium
google-genai
python-dotenv
scikit-learn
pypdf
pandas
pyarrow
tqdm
transformers
torch
accelerate

# web fetching
requests
cloudscraper
curl_cffi

# PDF parsing (optional but recommended)
pymupdf

# JS-rendered pages (optional, for Municode fallback)
playwright

# Selenium helper
webdriver-manager

# 4-bit quantisation for Gemma (optional)
bitsandbytes
